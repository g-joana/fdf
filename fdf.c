#include "fdf.h"
#include "libft/libft.h"
#include "stdio.h"

/*
int	main(int argc, char **argv)
{
	t_map	*map;

	if (argc == 2)
	{
		if (validate(argv[1]) == 0)
		 	return (1);
		map = generate_map(argv[1]);
		if (map == NULL)
			return (1);
		print_tab(map);
	//	generate_fdf(map);
	}
	(void)map;
	return (0);
}
*/
